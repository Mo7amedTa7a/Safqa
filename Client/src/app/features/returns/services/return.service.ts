// Member 5 - Return Service
// getReturns(filters?)     → GET   /api/returns
// getReturnById(id)        → GET   /api/returns/:id
// createReturn(data)       → POST  /api/returns
// approveReturn(id)        → PATCH /api/returns/:id/approve [Admin]
// rejectReturn(id, reason) → PATCH /api/returns/:id/reject  [Admin]
