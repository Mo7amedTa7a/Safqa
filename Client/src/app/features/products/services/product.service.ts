// Member 2 - Product Service
// getProducts(filters?)  → GET    /api/products
// getProductById(id)     → GET    /api/products/:id
// createProduct(data)    → POST   /api/products        [Admin]
// updateProduct(id,data) → PATCH  /api/products/:id   [Admin]
// deleteProduct(id)      → DELETE /api/products/:id   [Admin]
// getCategories()        → GET    /api/categories
