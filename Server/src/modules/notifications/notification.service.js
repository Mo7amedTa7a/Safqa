// Notification Service
// - Belongs to: Member 5
// - createNotification(recipientId, type, message, metadata): create a notification
// - getMyNotifications(userId): list all notifications for a user
// - markAsRead(notificationId, userId): mark notification as read
// - markAllAsRead(userId): mark all user notifications as read
// - Called internally by other services (deals, shipments, disputes, settlements)
