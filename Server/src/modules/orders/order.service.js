// Order Service
// - Belongs to: Member 4
// - createOrdersForDeal(dealId): create one Order per active PoolMember
// - getOrderById(id): get order details
// - getMyOrders(buyerId): buyer's order list
// - getSupplierOrders(supplierId): supplier's orders list
// - updateOrderStatus(id, status): transition order status
// - calculateOrderTotal(quantity, unitPrice, deliveryFee)
