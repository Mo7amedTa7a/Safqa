// Dispute Service
// - Belongs to: Member 5
// - createDispute(orderId, buyerId, data): open dispute within 48h window
// - reviewDispute(id, adminNote, resolution): admin reviews and resolves
// - getDisputesByOrder(orderId): get disputes for an order
// - getMyDisputes(buyerId): buyer's dispute list
